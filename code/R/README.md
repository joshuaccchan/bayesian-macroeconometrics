# R code (planned)

R ports of the MATLAB chapter code will live here, one folder per chapter
mirroring `code/matlab/` script-for-script (`linreg_t.m` -> `linreg_t.R`).
Ports are validated against the MATLAB output before they are added;
`code/PARITY.md` tracks per-script status. Chapters 1-4 (the sample chapters)
are first in line.
